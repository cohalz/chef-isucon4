worker_processes  1;

events {
    worker_connections  1024;
}

http {
    upstream app {
        server 10.0.0.243:8080;
    }

    server {
        location / {
            proxy_pass http://app;
        }
    }
}
